<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Nikolay Stoitsev]]></title>
  <link href="http://stoitsev.github.io/atom.xml" rel="self"/>
  <link href="http://stoitsev.github.io/"/>
  <updated>2015-03-01T01:26:58+02:00</updated>
  <id>http://stoitsev.github.io/</id>
  <author>
    <name><![CDATA[Nikolay Stoitsev]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Handling NaN in JavaScript]]></title>
    <link href="http://stoitsev.github.io/blog/2015/03/01/handling-nan-in-javascript/"/>
    <updated>2015-03-01T01:06:45+02:00</updated>
    <id>http://stoitsev.github.io/blog/2015/03/01/handling-nan-in-javascript</id>
    <content type="html"><![CDATA[<p>NaN is a special value representing something that is not a number. It may apear as a result of some illegal operation. For example:</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>var nanValue = 1 / 'a';</span></code></pre></td></tr></table></div></figure>


<p>After this expression is evaluated, the value of the variable <code>nanValue</code> will be <code>NaN</code>.</p>

<p>The fun starts here.</p>

<p>If you try to check the type of <code>nanValue</code> like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">typeof</span> <span class="nx">a</span><span class="p">;</span> <span class="c1">// &quot;number&quot;</span>
</span></code></pre></td></tr></table></div></figure>


<p>The result will be &ldquo;number&rdquo;.</p>

<p>Also, if you try to compare NaN to NaN, here is what happens:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="nx">nanValue</span> <span class="o">===</span> <span class="nx">nanValue</span><span class="p">;</span> <span class="c1">// false</span>
</span><span class='line'><span class="nx">nanValue</span> <span class="o">===</span> <span class="kc">NaN</span><span class="p">;</span> <span class="c1">// false</span>
</span><span class='line'><span class="nx">nanValue</span> <span class="o">==</span> <span class="kc">NaN</span><span class="p">;</span> <span class="c1">// false</span>
</span></code></pre></td></tr></table></div></figure>


<p>So, if you have to check for <code>NaN</code> value in some production code, you might write the following block:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">if</span> <span class="p">(</span><span class="nx">someValue</span> <span class="o">!==</span> <span class="kc">NaN</span><span class="p">)</span> <span class="p">{</span>
</span><span class='line'>  <span class="c1">// do something;</span>
</span><span class='line'><span class="p">}</span>
</span></code></pre></td></tr></table></div></figure>


<p>This condition will be always true. No matter if <code>someValue</code> contains <code>NaN</code> or not.</p>

<p>The correct way to do this is to use the built in function <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/isNaN">isNaN</a> or perform a special type of comparison.</p>

<p>The block of code, shown above, should be refactored like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">if</span> <span class="p">(</span><span class="nb">isNaN</span><span class="p">(</span><span class="nx">someValue</span><span class="p">))</span> <span class="p">{</span>
</span><span class='line'>  <span class="c1">// do something;</span>
</span><span class='line'><span class="p">}</span>
</span></code></pre></td></tr></table></div></figure>


<p>The second approach looks like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="k">if</span> <span class="p">(</span><span class="nx">someValue</span> <span class="o">!==</span> <span class="nx">someValue</span><span class="p">)</span> <span class="p">{</span>
</span><span class='line'>  <span class="c1">// do something;</span>
</span><span class='line'><span class="p">}</span>
</span></code></pre></td></tr></table></div></figure>


<p>This comparison will be true, only if the value of <code>someValue</code> is <code>NaN</code>.</p>

<p>In my humbe oppinion, the first approach is far more readable and should be prefered.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[JavaScript Inheritance Pitfall]]></title>
    <link href="http://stoitsev.github.io/blog/2015/02/28/javascript-inheritance-pitfall/"/>
    <updated>2015-02-28T23:16:07+02:00</updated>
    <id>http://stoitsev.github.io/blog/2015/02/28/javascript-inheritance-pitfall</id>
    <content type="html"><![CDATA[<p>Very often, while working on some JavaScript app, you will try to build a class hierarchy to encapsulate code in reusable and separate components. As you know, there are no native mechanisms in nowadays JavaScript, for creating classes and supporting inheritance. Usually people use some library that allows then to do so. Even when we use a popular battle-tested framework there are things that can go wrong. In the following post I am going to show you one of them.</p>

<p>I am going to use <a href="http://ejohn.org/blog/simple-javascript-inheritance/">Simple JavaScript Inheritance</a> library by John Resig(the creator of jQuery) for the following examples. Please, note that the problem described here, is not specific to this library.</p>

<p>Lets look at the following example.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="kd">var</span> <span class="nx">Person</span> <span class="o">=</span> <span class="nx">Class</span><span class="p">.</span><span class="nx">extend</span><span class="p">({</span>
</span><span class='line'>  <span class="nx">skills</span><span class="o">:</span> <span class="p">[</span><span class="s1">&#39;eat&#39;</span><span class="p">,</span> <span class="s1">&#39;sleep&#39;</span><span class="p">],</span>
</span><span class='line'>  <span class="nx">showSkills</span><span class="o">:</span> <span class="kd">function</span><span class="p">()</span> <span class="p">{</span>
</span><span class='line'>      <span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="k">this</span><span class="p">.</span><span class="nx">name</span> <span class="o">+</span> <span class="s1">&#39;: &#39;</span> <span class="o">+</span> <span class="k">this</span><span class="p">.</span><span class="nx">skills</span><span class="p">.</span><span class="nx">toString</span><span class="p">());</span>
</span><span class='line'>  <span class="p">}</span>
</span><span class='line'><span class="p">});</span>
</span><span class='line'>
</span><span class='line'><span class="kd">var</span> <span class="nx">Ninja</span> <span class="o">=</span> <span class="nx">Person</span><span class="p">.</span><span class="nx">extend</span><span class="p">({</span>
</span><span class='line'>  <span class="nx">init</span><span class="o">:</span> <span class="kd">function</span><span class="p">(){</span>
</span><span class='line'>    <span class="k">this</span><span class="p">.</span><span class="nx">skills</span><span class="p">.</span><span class="nx">push</span><span class="p">(</span><span class="s1">&#39;fight&#39;</span><span class="p">);</span>
</span><span class='line'>    <span class="k">this</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="s1">&#39;Ninja&#39;</span><span class="p">;</span>
</span><span class='line'>  <span class="p">}</span>
</span><span class='line'><span class="p">});</span>
</span><span class='line'>
</span><span class='line'><span class="kd">var</span> <span class="nx">Painter</span> <span class="o">=</span> <span class="nx">Person</span><span class="p">.</span><span class="nx">extend</span><span class="p">({</span>
</span><span class='line'>  <span class="nx">init</span><span class="o">:</span> <span class="kd">function</span><span class="p">(){</span>
</span><span class='line'>    <span class="k">this</span><span class="p">.</span><span class="nx">skills</span><span class="p">.</span><span class="nx">push</span><span class="p">(</span><span class="s1">&#39;paint&#39;</span><span class="p">);</span>
</span><span class='line'>    <span class="k">this</span><span class="p">.</span><span class="nx">name</span> <span class="o">=</span> <span class="s1">&#39;Painter&#39;</span><span class="p">;</span>
</span><span class='line'>  <span class="p">}</span>
</span><span class='line'><span class="p">});</span>
</span></code></pre></td></tr></table></div></figure>


<p>So, we have one base class <code>Person</code>. Also, there are <code>Ninja</code> and <code>Painter</code> that are extending <code>Person</code> by adding more skills to the <code>skills</code> array. So the ninja can eat, sleep and fight and the painter can eat, seep and paint.</p>

<p>Let&rsquo;s create one object of each type.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="kd">var</span> <span class="nx">ninja</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Ninja</span><span class="p">();</span>
</span><span class='line'><span class="kd">var</span> <span class="nx">painter</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Painter</span><span class="p">();</span>
</span></code></pre></td></tr></table></div></figure>


<p>And then let&rsquo;s try to call the <code>showSkills</code> method.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='javascript'><span class='line'><span class="nx">ninja</span><span class="p">.</span><span class="nx">showSkills</span><span class="p">();</span> <span class="c1">// outputs eat, sleep, fight, paint</span>
</span><span class='line'><span class="nx">painter</span><span class="p">.</span><span class="nx">showSkills</span><span class="p">();</span> <span class="c1">// outputs eat, sleep, fight, paint</span>
</span></code></pre></td></tr></table></div></figure>


<p>This is really weird. Why does <code>ninja.showSkills();</code> print <code>paint</code> as one of the ninja&rsquo;s skills? It has nothing to do with it because this skill is added only in the constructor of the <code>Painter</code> class. The states of those two objects should be isolated and some changes in the state of the first object should not change the state of the second object. This is opposite to what we see here. Stupid JavaScript.</p>

<p>If you don&rsquo;t believe me go <a href="http://jsfiddle.net/stoitsev/BjX3m/">try it</a>.</p>

<p>The reason for this strange behavior is that in JavaScript everything is passed by reference. Also, you have to remember that JavaScript uses functional scope. When you declare the <code>skills</code> array in the <code>Person</code> class definition, an object for that array gets created(because arrays are objects) and a reference for that object is saved in the <code>skills</code> variable.</p>

<p>When you create new instance of class <code>Painter</code>, the pointer to the <code>skills</code> object is copied and we access the same place in memory where the original array is. In the constructor of the object, we add <code>paint</code> as a skill to the array and because this point to the same original array declared in <code>Person</code>, we actually modify it.</p>

<p>When you create second instance of class <code>Ninja</code> the same thing happen. In the constructor of the class we access the same array declared in <code>Person</code> and when we add <code>fight</code> as an element to it, we actually add it to the original array again. So after the two objects are created, <code>paint</code> and <code>fight</code> are added to the same <code>skills</code> array.</p>

<p>When you try to print the <code>skills</code> array from some of the instance, you access the object that is declared in the <code>Person</code> class, which contains both <code>paint</code> and <code>fight</code>. That&rsquo;s why they are printed as a result of <code>showSkills</code> call.</p>

<p>If you want to see a solution to the problem, <strong>ask in the comments bellow</strong> and I&rsquo;ll post it over here.</p>

<p>This is my first blog post in this new blog so stay tuned for more.</p>
]]></content>
  </entry>
  
</feed>
